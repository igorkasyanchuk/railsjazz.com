= title @project.title

%h1.left #{@project.title}
.add.right
  = link_to "Edit", [:edit, :admin, @project, @screenshot], :class => 'edit_button'  
.clear

%p
  = link_to "View All Project Sreenshots", [:admin, @project, :screenshots], :class => 'index_button'  

%p
  Priority: #{@screenshot.priority}

%p
  = @screenshot.description
  %br
  = image_tag @screenshot.image.url

%p
  = link_to "View All Project Sreenshots", [:admin, @project, :screenshots], :class => 'index_button'  
